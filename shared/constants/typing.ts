/**
 * 타이핑 모드 상수.
 *
 * LISTENING_QUESTION_COUNT 와 같은 층에 둔다 — daily 라우트가 셋을 나란히 써서
 * readingCount = count - 듣기 - 쓰기 를 계산한다.
 */

/**
 * 데일리 퀴즈 한 세션에 낼 타이핑 문항 수.
 *
 * 리스닝과 달리 쿼리 파라미터로 조절하지 않는다(끄고 싶다는 요구가 아직 없다).
 * 이 상수를 0 으로 내리는 것이 배포 롤백 없는 킬 스위치다.
 *
 * 2 인 이유: 타이핑은 문항당 체감 시간이 4지선다의 2~3배다(철자 중앙값 8자 입력).
 * 총 10문항을 유지하면서 세션이 길어지는 것을 억제한다.
 */
export const TYPING_QUESTION_COUNT = 2;
