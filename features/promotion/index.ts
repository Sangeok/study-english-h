/**
 * Promotion Feature - Public API
 *
 * 페이지가 쓰는 표면만 노출한다(features/quiz 배럴 관례).
 * 훅·api 는 슬라이스 내부 구현이라 밖으로 내보내지 않는다.
 */
export { PromotionTest } from "./ui/promotion-test";
export { PromotionErrorBoundary } from "./ui/promotion-error-boundary";
